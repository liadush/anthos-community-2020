apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: role:fake-user
rules:
- apiGroups: ['policy']
  resources: ['podsecuritypolicies']
  verbs:     ['use']
  resourceNames:
  - example
